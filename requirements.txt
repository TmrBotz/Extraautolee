# URL Uploader Bot — Dependencies

# MTProto Framework (Rust WarpCrypto — fastest)
wzgram[fast]>=3.0.20

# Async HTTP client
aiohttp>=3.9.0

# Faster event loop (optional but recommended)
uvloop>=0.19.0; sys_platform != "win32"

# MongoDB async driver
motor>=3.3.0

# HTML parser
beautifulsoup4>=4.12.0
lxml>=5.0.0
